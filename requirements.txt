# ========= 核心运行依赖 =========

weixin-auto
requests>=2.31.0
psutil>=5.9.6
pyautogui>=0.9.54

# ========= wxauto 依赖链（显式写出，避免 PyInstaller 漏收） =========

pywin32>=306
pywinauto>=0.6.8
uiautomation>=2.0.20
keyboard>=0.13.5
mouse>=0.7.1

# ========= 打包工具（仅开发机需要） =========

pyinstaller>=6.3.0
